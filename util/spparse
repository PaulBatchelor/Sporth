# spparse
# By Paul Batchelor
# This shell utility formats sporth code to be sent over OSC via oscsend

PORT=$1

str=""

while read line 
do
    str=$(printf "$str\n$line")
    echo "$line"
done

oscsend localhost $PORT /sporth/eval s "$str"
